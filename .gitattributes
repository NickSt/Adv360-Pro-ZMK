# Shell scripts run inside the Linux build container: they must keep LF
# endings even when checked out on Windows with core.autocrlf=true.
*.sh text eol=lf
Dockerfile text eol=lf
Makefile text eol=lf
